<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

    <persistence-unit name="cadastroPessoas" transaction-type="RESOURCE_LOCAL">

        <!-- Define a implementação JPA -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- ENTIDADES DO PROJETO -->
        <class>com.cadastropessoas.model.Pessoa</class>
        <!-- Adicione mais entidades aqui, se houver -->

        <!-- Configuração do banco de dados -->
        <properties>
            <!-- Conexão com PostgreSQL -->
            <property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/cadastropessoas"/>
            <property name="javax.persistence.jdbc.user" value="postgres"/>
            <property name="javax.persistence.jdbc.password" value="admin"/>

            <!-- Dialeto do Hibernate para PostgreSQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- Mostrar SQL no console -->
            <property name="hibernate.show_sql" value="true"/>

            <!-- Formatar SQL -->
            <property name="hibernate.format_sql" value="true"/>

            <!-- Atualizar tabelas automaticamente -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Nome do schema (opcional) -->
            <!-- <property name="hibernate.default_schema" value="public"/> -->

        </properties>

    </persistence-unit>
</persistence>
